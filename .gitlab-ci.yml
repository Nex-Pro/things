stages:
    - deploy

pages:
    stage: deploy
    image: alpine:latest
    script:
        - apk add nodejs brotli
        - node generate-index.js
        - node generate-brotli.js
        - mkdir .public
        - cp -r * .public
        - mv .public public
    artifacts:
        paths:
            - public
